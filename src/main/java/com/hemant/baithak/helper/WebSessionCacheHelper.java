package com.hemant.baithak.helper;

public class WebSessionCacheHelper {

  // meets -> set(meetId)
  // session:{sessionId} -> websession, TTL
  // sessions:meet:{meetId} -> set(session)
  // session:{session}:user -> name

}
